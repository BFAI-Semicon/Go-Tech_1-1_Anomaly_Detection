# Worker dependencies
# Note: torch, torchvision, opencv-python, pyyaml are pre-installed in nvcr.io/nvidia/pytorch:25.11-py3
# Anomalib automatically installs: lightning, kornia, timm, FrEIA, omegaconf, etc.
anomalib
# Avoid pkg_resources removal warning (some dependencies still use it)
setuptools<81
# Use headless version to avoid conflicts with base image opencv-python
opencv-python-headless
# LeadersBoard specific dependencies
mlflow
redis
python-dotenv
